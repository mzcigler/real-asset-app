// Re-export from the canonical types location for backward compatibility.
// Prefer importing directly from '@/types' in new code.
export type { TaskType, Property, DBTask, TaskRow, FileRecord } from '@/types';
