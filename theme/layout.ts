/**
 * Global layout constants.
 * Change these values to affect the whole app layout in one place.
 */

/** Maximum width for form content (auth screens, profile form block) */
export const MAX_WIDTH = 480;

/** Uniform maximum content width for ALL main pages — keeps page edges aligned when navigating */
export const PAGE_MAX_WIDTH = 1120;

/** Standard horizontal padding on content screens */
export const SCREEN_PADDING = 24;

/** Width breakpoint (px) between phone-style and desktop-style content layouts */
export const BREAKPOINT = 768;

/** Width of the persistent sidebar (SideNav) on wide windows */
export const SIDEBAR_WIDTH = 256;

/** Window width (px) at which the sidebar stays open instead of collapsing behind the hamburger */
export const SIDEBAR_BREAKPOINT = 1024;
